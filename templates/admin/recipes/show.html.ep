% layout 'mealmaps', title => $recipe->{title};

% content headline => '';
% content main_form => '';
% content overlay => '';

% content solid => begin
<h2><%= $recipe->{title} %></h2>
<p><%= link_to 'View PDF' => "/recipes/$recipe->{id}.pdf" %></p>
<p><%= image "recipes/$recipe->{id}.png", width => 300 %></p>
%= link_to 'Edit' => edit_recipe => {id => $recipe->{id}}
% end
